// Replace 'localhost' with your computer's local IP address (e.g., '192.168.1.5') 
// if you are testing on a physical device or emulator that can't see 'localhost'.
export const API_URL = 'http://localhost:5000/api';
